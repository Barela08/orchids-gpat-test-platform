kind = "web"
previewPath = "/"
title = "GPAT Exam Portal"
version = "1.0.0"
id = "artifacts/gpat-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18431

[services.development]
run = "pnpm --filter @workspace/gpat-portal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/gpat-portal", "run", "build" ]
publicDir = "artifacts/gpat-portal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18431"
BASE_PATH = "/"
